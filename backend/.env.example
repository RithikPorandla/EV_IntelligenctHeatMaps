# Backend
ENV=dev
CORS_ALLOW_ORIGINS=http://localhost:3000

# Postgres (docker-compose default)
DATABASE_URL=postgresql+psycopg2://postgres:postgres@db:5432/evchargemap

# ML model artifact path (JSON)
MODEL_PATH=models/site_demand_model.json
