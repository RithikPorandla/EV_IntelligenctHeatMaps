import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'MA EV ChargeMap - EV Charging Siting Intelligence',
  description: 'Data-driven EV charging infrastructure siting for Massachusetts cities. A portfolio project demonstrating data analysis, ML, and full-stack development.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
